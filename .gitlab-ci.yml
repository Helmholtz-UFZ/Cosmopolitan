stages:
  - build

build_image:
  stage: build
  image: docker:latest
  script:
    - docker build --build-arg GIT_PAT_SM="$GIT_PAT_SM"  --build-arg GUNICORN=1 --build-arg PORT=8080 -t cosmopolitan-test .
  tags:
    - "hifis"

